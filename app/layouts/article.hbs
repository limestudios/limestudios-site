{{>open}}

<body id="page-top">
  {{> nav }}
  {{> article-header }}
  <section class="article-body {{this.title}} row row-widetablet">
    {{#markdown}}
      {{> body}}
    {{/markdown}}
  </section>
  {{> social-media}}
  {{> footer }}
  {{> scripts }}
</body>

{{>close}}
